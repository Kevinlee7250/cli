node_modules/
output/
logs/*.log
.env
*.png
*.jpg

__pycache__/
*.pyc
*.pyo

# 실행 1회분 산출물 — 커밋하면 안 됩니다.
# 워크플로가 "이번 실행에 발행이 있었나"를 이 파일로 판단해 사이트맵
# 재제출 여부를 정합니다. 커밋해 두면 main.py가 쓰기 전에 죽었을 때
# 지난 실행의 값을 이번 실행 결과로 읽어, 발행 0건인데 제출하게 됩니다.
logs/last_publish_summary.json
