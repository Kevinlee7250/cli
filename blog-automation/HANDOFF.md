# 인수인계 — 2026-08-19 (3차)

새 세션이 이 문서만 읽고 이어갈 수 있도록 정리했습니다.

- 저장소: `Kevinlee7250/cli` (npm/cli의 **공개 포크**)
- 브랜치: `latest` 하나만 씁니다 (예전 작업 브랜치 `claude/blog-automation-system-KAbtE`는
  2026-08-25에 정리했습니다 — 워크플로가 읽는 코드와 쓰는 대상이 어긋나던 원인이었습니다)
- 테스트: `cd blog-automation && python -m pytest tests/ -q` → **434건 통과** (2026-08-21)

---

## ⛔ 절대 지켜야 할 제약

1. **이 저장소는 공개입니다.** `GOOGLE_REFRESH_TOKEN`, `GOOGLE_CLIENT_SECRET`을
   Actions 로그·아티팩트·커밋에 절대 노출하지 마세요. OAuth 코드→토큰 교환은
   **로컬에서만** 합니다. (이 규칙을 어기던 `get-oauth-token.yml`은 삭제했습니다.)
   `client_id`는 공개 식별자이므로 노출돼도 무방합니다.

2. **`GOOGLE_CLIENT_ID`를 바꾸지 마세요.** 현재 값
   (`310114393483-rtotnsftmbbat8l3pua3suer3juv91o6...`)은 정상 동작 중입니다.
   같은 프로젝트에 클라이언트가 2개 있어 콘솔에서 다른 값(`gtn7a...`)이 보이는데,
   그건 별개 클라이언트입니다. 로컬에서 `invalid_client`가 나는 건 ID와 secret을
   잘못 짝지었기 때문입니다. 근거: `docs/data/auth_health.json` → `ok=true`
   (실제 refresh_token 교환 성공).

3. **워크플로의 `schedule`·`workflow_dispatch`는 기본 브랜치(`latest`)의
   파일만 인식합니다.** 기능 브랜치에만 워크플로를 추가하면 Actions 목록에
   "This workflow does not exist"가 뜹니다. 워크플로는 `latest`에 올리세요
   (잡 안에서도 `ref: latest`로 체크아웃합니다 — 읽는 코드와 쓰는 대상이
   같은 브랜치여야 합니다).

4. **`docs/.gitignore`(npm/cli 템플릿)가 `docs/*`를 무시합니다.**
   워크플로에서 docs 파일을 커밋하려면 반드시 `git add -f`.

5. 테스트가 `blog-automation/logs/used_images.json`을 오염시킵니다.
   커밋 전 `git checkout -- blog-automation/logs/used_images.json`.

6. **사이트맵 API의 `indexed` 필드를 색인률로 읽지 마세요.** 구글이 지원을
   중단한 값이라 **항상 0**입니다. `gsc_diagnosis.json`에서는
   `indexedUrlsDeprecated`로 이름 붙이고 `note`에 경고를 실어 두었습니다.
   색인 여부는 `index_status.json`(URL 검사 API)이나 Search Console 화면으로
   판단하세요. "제출 421 / 색인 0 = 0%"는 틀린 계산입니다.

7. **색인 요청 자동화 코드를 만들지 마세요.** Google Indexing API는 문서상
   채용공고·방송이벤트 전용이고, Search Console API에는 색인 요청 엔드포인트가
   없습니다. 웹 UI 버튼뿐이며 속성당 하루 10건 내외입니다.
   또한 **`/search-console/inspect?resource_id=…&id=…` 딥링크는 404가 납니다**
   (2026-08-19 실제 확인). 공식 형식이 아니니 다시 시도하지 마세요.

---

## 📊 현재 상태 (2026-08-19 기준)

| 항목 | 시작 | 지금 |
|---|---:|---:|
| 하루 발행량 | 9.2건 | **1건** (08-20 PR #14 머지 후 실제 적용) |
| 블로그 간 주제 중복 | 6개 | **0개** |
| 무단 저작권 이미지 | 351장 | **0장** |
| 잘못된 출처 표기 | 106건 | **0건** |
| 실제 색인 (blog2 GSC 화면) | 0건 | **4건** |
| AdSense 승인 확률(추정) | 5~10% | **30~40%** |

블로그 3개: blog1 `www.hoguwhat.com` / blog2 `hogugolfntraval.blogspot.com` /
blog3 `hoguasset.blogspot.com`. 총 발행 약 478건, 운영 약 2개월.

---

## 🎯 지금 풀고 있는 문제: AdSense 승인

AdSense는 **미승인**이며 **신청 보류 중**입니다. 색인률이 회복되기 전에
신청하면 거절되고, 거절 이력이 쌓이면 재승인이 더 어려워집니다.

### 갖춰진 것 (건드리지 마세요)

| 요건 | 상태 |
|---|---|
| 소개·개인정보처리방침·문의 페이지 | ✅ 3개 블로그 전부 |
| 글자수 (중앙값 3,335자) | ✅ 요건 1,000자 |
| 목차·FAQ·구조화 데이터 | ✅ 100% |
| ads.txt · 사이트맵 | ✅ 정상 |
| 이미지 저작권 | ✅ 100% 안전 |

### 남은 병목: 색인 — **설정 문제 아님이 확인됨** (2026-08-19 실행 #40)

`gsc_diagnose.py` 첫 실행 결과 **문제 0건**입니다:

| 블로그 | GSC 속성 | 사이트맵 |
|---|---|---|
| HOGU What? | `sc-domain:hoguwhat.com` | 등록·크롤 정상 (08-18) |
| HOGU 여행,스포츠,연예 | `https://hogugolfntraval.blogspot.com/` | 정상 (08-19) |
| 금융NEWS | `https://hoguasset.blogspot.com/` | 정상 (08-19) |

- **쓰기 권한 `canWrite: true`** — 1차 세션의 "사이트맵 권한 부족" 가설 최종 반증
- **blog1이 실제로 도메인 속성** — `gsc_fetcher` 폴백 수정(7번)이 옳았음을 확인
- 사이트맵 오류 0, 제출 URL 421개

표본 편향을 고친 뒤 검사 대상이 07-25 ~ 08-19로 고르게 퍼졌고, 그 결과는
**색인 0 / 20**(Discovered 16 · URL unknown 4)입니다. 3.5주 지난 글도
색인되지 않았으므로 이제 이 0은 "신규 글이라 아직"이 아닌 실제 상태입니다.
GSC 화면 수치(blog2 색인 4 / 미색인 130 ≈ 3%)와도 일관됩니다.

즉 병목은 설정이 아니라 **구글의 크롤 예산 배정**입니다.
`Discovered - currently not indexed`가 다수인 것이 그 신호이며, 보통
사이트 신뢰도가 낮을 때(신생·백링크 없음·대량 자동 발행 이력) 나타납니다.
발행량 감축(08-19) 효과가 나타나는 데 2~4주가 필요합니다.

---

## 🔜 다음 할 일

### 1. ~~`index_priority.py` 재업로드~~ → **완료** (2026-08-20, `5fb5523c`)

누락이 사실이었고 `site`·`gscProperty`를 모두 내보내도록 고쳤습니다.
현재 `index_status.json`에 `gsc_property` 키가 없는 것은 파일이 해당
변경 이전에 생성됐기 때문이며, 다음 색인 점검부터 채워집니다.

### 2. 매일 색인 요청 10건 (사용자 작업)

대시보드 → 시스템 관리 → 구글 색인 현황 위 **색인 요청 패널**
→ `▶️ 다음 URL 열기` → GSC 검사창에 `Ctrl+V` → Enter → **색인 생성 요청**.
진행 상황은 localStorage에 저장되고 날짜가 바뀌면 자동 초기화됩니다.

### 3. ~~`docs/index (1).html` 삭제~~ → **완료** (2026-08-20, `5fb5523c`)

`docs/index.html`과 바이트 단위로 동일함을 확인하고 삭제했습니다.

### 4-A. 내부 링크 — **측정 완료, 도구 제작 완료** (2026-08-21)

측정 결과 (`docs/data/internal_links.json`, 실행 #42):

| | |
|---|---:|
| 발행 글 | 431건 |
| **고아 페이지** | **175건 (40.6%)** |
| 인바운드 평균 | 2.44 (최대 17) |

블로그별 34~44%로 고르고 최근 고아 50건은 전부 8월 발행분 —
구조 가설(새 글은 인바운드 0)은 **사실로 확인**됐습니다.

**그러나 색인의 원인이라는 근거는 못 찾았습니다:**

```
고아    / 미색인 : 10건
링크있음 / 미색인 : 10건
```

인바운드가 있는 글도 똑같이 미색인입니다. 다만 표본 20건이 전부
미색인이라 **대조군이 없어 인과를 판정할 수 없습니다**. "고아라서
색인이 안 된다"도 "관계없다"도 이 데이터로는 말할 수 없습니다.

→ `inbound_linker.py` + `inbound-links.yml`(PR #16)을 만들되,
**색인 해결책이 아니라 사이트 구조상 정당한 개선**으로 위치를 잡았습니다.
색인이 풀린다고 주장하지 마세요.

실행: 대시보드 시스템 탭 → 내부 링크 구조 → 미리보기 / 링크 생성
(또는 Actions → 🔗 역방향 내부 링크). 주 1회 토요일 08시 KST 자동.

### 4-A-old. (참고) 원래 가설 서술

`internal_linker`는 글 생성 시점에 **한 방향으로만** 돕니다:

```
새 글 ──링크──▶ 기존 글 (최대 5개)     있음
기존 글 ──링크──▶ 새 글                만드는 코드가 저장소에 없음
```

즉 모든 새 글이 **들어오는 내부 링크 0개**로 태어난다는 가설입니다.
사이트 안에서 아무도 가리키지 않는 페이지는 크롤 예산을 받을 근거가 약해
`Discovered - currently not indexed`의 대표적 원인이 됩니다.

**아직 가설이므로 먼저 측정합니다.** `internal_link_audit.py`(읽기 전용)를
`gsc-indexing.yml`에 붙였고 PR #15로 기본 브랜치에 반영했습니다.
다음 색인 점검에서 `docs/data/internal_links.json`이 생성됩니다.

판단 기준:
- **고아 비율이 높고 색인 상태와 상관이 보이면** → 역방향 내부 링크 도구 제작
  (기존 글 2~3개를 수정해 새 글로 링크 삽입, enrich_posts와 같은 방식)
- **고아가 적으면** → 가설이 틀린 것. 소셜 발행 등 다른 경로를 검토

### 4-B. 2~4주 후 색인 재점검

Actions → `🔍 구글 색인 점검` 실행 → 색인률 확인.
**색인률 15~30% 도달 시 AdSense 재신청 검토.**

### 5. ~~PR #12 머지~~ → **머지 완료** (2026-08-20, `8ac57400`)

기본 브랜치에 반영된 것:
- 신규: `pending-cleanup.yml`(주 1회), `alert-watchdog.yml`(6시간마다)
- 수정: `blog-analytics.yml`(`git add -f` + 수익 수집), `gsc-indexing.yml`(진단 + 색인 우선순위)
- 삭제: `get-oauth-token.yml` — 공개 저장소에서 refresh_token을 교환·업로드하던 경로

이제 위 스케줄이 실제로 돕니다. 머지 전 `blog-run.yml`(발행량 감축)과
`fix-unlicensed-images.yml`이 되돌아가지 않는지 파일 단위로 확인했습니다.

### 6. 기존 발행글의 주제 중복 → **보류 결정** (2026-08-20)

조사 결과 **실제 중복 콘텐츠가 아니었습니다.** blog1·blog2 발행글의
제목 완전 일치 **0건**, 키워드 중복 **1건**(추석 선물세트)뿐입니다.
겹치는 것은 카테고리일 뿐 서로 다른 글이므로 중복 콘텐츠 정책 대상이
아닙니다.

비공개 처리 시 규모(post_registry 최근 300건 기준, 그 이전 글 미집계):

| blog2 분류 | 건수 |
|---|---|
| 여행 (주제 이탈) | 65 |
| 건강·일반 (주제 이탈) | 6 |
| 스포츠 (주제 부합) | 9 |

주제 이탈분을 모두 내리면 blog2에 **9건만** 남아 "콘텐츠 부족"으로
AdSense 거절 위험이 오히려 커집니다. 사용자 판단으로 **보류**하고
색인 회복을 우선하기로 했습니다. 신규 발행은 이미 주제별로 분리돼
있으므로 시간이 지나면 비중이 자연히 정상화됩니다.

⚠️ `migrate_offtopic.py`는 **blog1 전용이며 주제 키워드가 하드코딩**돼
있습니다. 그 정의는 blog1이 여행+연예를 함께 다루던 시절 것이라 현재
설정(blog1=여행 / blog2=스포츠·연예)과 다릅니다. 그대로 실행하면
오분류하므로, 쓰려면 먼저 blogs.json의 topics를 읽도록 고쳐야 합니다.

### 7. ~~blog1의 GSC 데이터 누락~~ → **완료** (2026-08-20, `4d54f49f`)

원인은 `gsc_fetcher.py`에만 도메인 속성 폴백이 없던 것이었습니다.
`blog_analytics`·`gsc_indexing`에는 있는 `sc-domain:` 재시도가 빠져 있어
커스텀 도메인인 blog1이 403을 받고 그대로 포기했습니다. 색인 검사에서는
blog1이 정상 조회되고 있어(7건) 문제가 이 모듈에 한정됨을 확인했습니다.
다음 GSC 수집부터 `gsc.json`에 blog1이 포함됩니다.

---

## 🛠️ 이 세션(3차, 2026-08-19)에서 한 것

| 영역 | 내용 |
|---|---|
| **발행량** | `blog-run.yml` cron 3회→1회 + `rotation` 잡 추가. 연중일 % 3으로 당번 블로그 1개만 발행 → 9.2건/일 → 1건/일 |
| **주제 분리** | `blogs.json` — blog1=여행·맛집·숙소·캠핑, blog2=스포츠·골프·연예, blog3=금융. 교집합 0 |
| **이미지 저작권** | `image_fetcher.py`에 화이트리스트 도입. 네이버·DDG를 검색 경로에서 **제거**. Pixabay→Wikimedia만 사용, 실패 시 AI 생성→SVG 썸네일 폴백 |
| **소급 정리** | `fix_unlicensed_images.py` 신규 — 3회 실행으로 무단 이미지 321장 교체, 캡션 106건 정정. 최종 전수 검사 0건 |
| **캡션 동기화** | `sync_captions()` — figure의 img src를 보고 출처 표기를 다시 씀. 화이트리스트 밖이면 출처 줄 삭제 |
| **색인 패널** | 대시보드에 원클릭 색인 요청 패널. 주소 복사 + GSC 열기(탭 재사용) + 진행률 + 날짜별 자동 초기화 |
| **진단 문서** | `INDEXING_DIAGNOSIS.md`, `ADSENSE_AUDIT_20260819.md` 신규 |
| **테스트** | 322 → **393건** (이미지 라이선스 45건, 교체 도구 26건 추가) |

### 새로 만든 파일

```
blog-automation/fix_unlicensed_images.py          # 무단 이미지 소급 교체
blog-automation/tests/test_image_license.py       # 화이트리스트 회귀
blog-automation/tests/test_fix_unlicensed_images.py
blog-automation/INDEXING_DIAGNOSIS.md             # 색인 병목 진단
blog-automation/ADSENSE_AUDIT_20260819.md         # 전체 점검 보고서
.github/workflows/fix-unlicensed-images.yml       # (latest에도 등록됨)
```

---

## 🧭 진단 이력 — 반증된 가설들

같은 실수를 반복하지 않도록 남깁니다.

| 가설 | 누가 | 반증 근거 |
|---|---|---|
| client_id가 낡아서 인증 실패 | 1차 세션 "가장 유력" | `auth_health.json` → 실제 토큰 교환 성공 |
| 사이트맵 쓰기 권한 부족 | 1차 세션 후보 B | 사이트맵 3개 정상 서빙, 478 URL 등재 |
| 색인 0/20 = 심각한 장애 | `index_status.json` | 표본이 전부 1~2일 된 글. 실제 GSC는 색인 4건 |
| FAQ 없는 58건 = 생성 버그 | 2차 세션 | 대부분 07-20 이전 레거시 글 |
| GSC inspect 딥링크로 직행 가능 | 3차 세션 (내 실수) | 실제로 눌러보니 404 |
| blog1·blog2 주제 중복이 정책 문제 | 3차 세션 | 제목 일치 0건·키워드 중복 1건 — 카테고리만 겹침 |
| 사이트맵 "제출 421 / 색인 0" = 색인률 0% | 4차 세션 (하마터면) | `indexed`는 구글이 지원 중단한 필드로 항상 0 |
| 발행량 감축이 08-19부터 적용 중 | 3·4차 세션 | 기능 브랜치에만 있어 미적용 — 08-20까지 하루 8~11건 계속 발행 |
| 재검증 미달 30건 = 콘텐츠 품질 문제 | pending-cleanup 첫 실행 | `word_count` 미전달로 전부 "0자" 판정 (실제 중앙값 2,420자) |
| 고아 페이지가 미색인의 원인 | 4차 세션 | 링크 있는 글 10건도 똑같이 미색인 — 인과 근거 없음 |
| 이미지 링크 깨짐 227건 | `image_audit` 첫 실행 (내 버그) | 전부 `data:image/svg+xml` 인라인 썸네일. `startswith("http")` 검사가 data URI를 오판 |
| suspect 15건은 alt가 비어서 | 5차 세션 (내 실수) | 리포트에 `alt` 필드가 아예 없었음 — `i.get('alt')`의 None을 "alt 없음"으로 오독 |
| 시스템이 정상 (모든 지표 초록) | 5차 세션 전체 점검 | `automation.log`를 안 열었음. 실제로는 콘텐츠 생성 100% 실패로 이틀간 발행 0건 |

**교훈 0 — 워크플로 success를 정상으로 읽지 마세요.**
2026-08-22 점검에서 확인된 것:
- `blog-run`: 콘텐츠 생성이 100% 실패해도 success (main.py가 0을 반환)
- `blog-verify`: 38회 실행 38회 실패, 08-13 이후 트리거조차 안 됨
- 두 경우 모두 실패 알림 없음, 대시보드 표시 없음

점검할 때는 반드시 `blog-automation/logs/automation.log`와 실행 **소요 시간**을
보세요. blog-run이 35분에서 3분으로 줄어 있으면 아무것도 발행하지 않은 것입니다.

**교훈 1**: 산출물(`docs/data/*.json`)과 라이브 페이지를 직접 확인하기 전에
원인을 단정하지 마세요. 이 저장소에서 첫 가설이 반복해서 틀렸습니다.

**교훈 2**: 워크플로를 고쳤으면 **기본 브랜치에 실제로 반영됐는지 확인**하세요.
제약 3번을 알고 있으면서도 발행량 감축이 하루 넘게 미적용 상태로 방치됐습니다.
확인 명령: `git show origin/latest:.github/workflows/<파일> | grep <바뀐 줄>`

**교훈 3**: "미달 N건" 같은 집계를 보면 **사유를 한 번은 펼쳐 보세요**.
재검증 30건 전부 미달은 품질 문제가 아니라 필드 하나가 빠진 결과였습니다.

---

## 💡 아직 유효한 관찰

- **추정 수익이 자릿수 단위로 과대**: 함의 RPM $38.61. 한국어 블로그 실제
  RPM은 $0.5~3. AdSense 승인 후 실측이 들어와야 정상화됩니다.
  **이 수치를 의사결정 근거로 쓰지 마세요.**
- 대기 글(`pending_posts.json`) 160건 적체. 자동 발행되지는 않음.
- `auto-repair.yml`이 매일 02시 KST에 실패 글을 최대 5건 재시도합니다 —
  발행량 감축을 우회할 수 있으니 색인 회복기에는 상한 축소 검토.
- 외부 링크(백링크) 사실상 0. 다만 **자동화할 안전한 방법이 없습니다**:
  링크 구매·교환·PBN·자동 댓글은 링크 스팸 정책 위반이라 지금 상태에서
  적발되면 AdSense가 더 멀어집니다. Search Console API에는 백링크
  엔드포인트도 없어(웹 UI 전용) 추적조차 자동화되지 않습니다.
  **통제 가능한 내부 링크(4-A)를 먼저 처리하는 것이 순서입니다.**
- 소셜 발행은 콘텐츠가 이미 생성되고 있으나 **토큰 미설정으로 발행이 꺼져
  있습니다**(40건 skipped). 대부분 nofollow라 링크 가치는 낮고 발견·유입
  경로로만 유효합니다. 켜려면 INSTAGRAM_ACCESS_TOKEN / INSTAGRAM_ACCOUNT_ID,
  THREADS_ACCESS_TOKEN / THREADS_USER_ID 가 필요합니다.

---

## ⚙️ 이 세션의 제약 (Cowork)

3차 세션은 저장소 쓰기 권한이 없어 **모든 변경을 파일로 전달하고 사용자가
GitHub 웹에서 업로드**했습니다. 그 과정에서 반복된 사고:

- 다운로드 시 파일명이 바뀜 (`blog-run.yml` → `blogrun.yml`,
  `index.html` → `index (1).html`) → 새 파일이 생기고 원본은 안 바뀜
- 같은 이름 파일이 여러 번 다운로드되어 옛 버전이 올라감

**저장소가 연결된 세션(데스크탑 앱 `</> Code`)에서 작업하면** 에이전트가
직접 커밋·푸시하므로 이 문제가 없습니다. 가능하면 그쪽을 쓰세요.

---

## 빠른 참조

| 작업 | 위치 |
|------|-----|
| 대시보드 | https://kevinlee7250.github.io/cli/ |
| Actions | https://github.com/Kevinlee7250/cli/actions |
| Secrets | https://github.com/Kevinlee7250/cli/settings/secrets/actions |
| Search Console | https://search.google.com/search-console |
| 색인 점검 실행 | Actions → 🔍 구글 색인 점검 |
| 무단 이미지 검사 | Actions → 🔒 무단 이미지 교체 (`dry_run` 체크) |
| 로컬 테스트 | `cd blog-automation && python -m pytest tests/ -q` |
