anthropic>=0.40.0
openai>=1.30.0
requests>=2.31.0
python-dotenv>=1.0.0
schedule>=1.2.0,<2.0.0
json-repair>=0.30.0
pytrends>=4.9.0
google-api-python-client>=2.100.0
google-auth>=2.20.0
google-auth-httplib2>=0.2.0

# 이미지 내용 지문(dHash) — fix_duplicate_images가 씁니다.
# 지금까지 다른 패키지에 딸려 설치돼 동작했지만 선언은 없었습니다. 상위
# 패키지가 Pillow를 떼면 `except ImportError: return None`으로 빠져
# URL 비교만 남고, 워크플로는 그대로 성공으로 끝납니다.
Pillow>=10.0.0
